#include <stdio.h>
#include <stdlib.h>
#define size 10

struct node {
    
}

struct ll {
    struct ll* next;
    int vertex;
};

struct ll* ar[size];

void addEdge(int src, int dest){
    ar[src]
}

int main(){
    for(int i = 0; i < 10; i++){
        ar[i] = (struct ll*)malloc(sizeof(struct ll)*100);
    }


    return 0;
}